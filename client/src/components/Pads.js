import React, { Component } from 'react';
import Pad from './Pad'
class Pads extends Component {
  // renderPads = () => this.props.sounds.currentSounds.map((sound) => <Pad sound={sound} key={sound.id} url={sound.url} name={sound.name} />)

  render() {
    // debugger
    return(
      <ul>
      </ul>
    );
  }
};

export default Pads;
